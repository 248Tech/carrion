// Package main is the mg7d ctl CLI (stub for Phase 0-3).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stdout, "mg7d ctl (stub); status endpoint not implemented in Phase 0-3)")
	os.Exit(0)
}
